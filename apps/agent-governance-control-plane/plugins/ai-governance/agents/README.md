# Agents — deferred for this pilot

The `ai-governance` pilot ships skills only in this MVP; no agent
capability is registered. See ADR §15 ("Explicit out-of-scope items") for
why `agent_definitions` exists in the database schema but has no row for
this plugin yet.
