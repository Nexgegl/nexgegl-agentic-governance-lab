# CLAUDE.md — [اسم المستودع / Repo Name]

> هذا الملف هو الطبقة الثالثة (المحلية) من تسلسل تثبيت التعليمات المعرَّف في:
> `claude-operating-system/00-master-standards/NEXGEGL_CLAUDE_MASTER.md`
>
> **الترتيب الملزم: Master Standard → Product Profile → هذا الملف (Local Repo).**
> لا يجوز لأي تعليمة هنا أن تُخفف أو تُعيد تعريف أي مبدأ حاكم وارد في الطبقتين الأعلى.

---

## 1. تصنيف هذا المستودع (Repository Classification)

> اختر واحداً وامسح الباقي. يحدد هذا التصنيف مستوى الصرامة المطلوب من كل Sub-Agent يعمل هنا.

- [ ] Core IP Repo — ملكية فكرية أساسية (منطق SDGM/KFSA الجوهري)
- [ ] Product Repo — كود منتج تجاري قائم
- [ ] Client Repo — بيئة/تخصيص عميل
- [ ] Experiment Repo — بيئة بحث وتقييم محكومة

## 2. Product Profile المرتبط

> إن كان هذا المستودع تابعاً لمنتج معرّف، أشر إلى ملف البروفايل:
> `claude-operating-system/02-product-profiles/<product>/CLAUDE.<product>.md`

---

## 3. نظرة عامة على المشروع (Project Overview)

- **الغرض:** [صف بإيجاز ماذا يفعل هذا المستودع]
- **المكدس التقني (Tech Stack):** [اللغات، الأطر، قواعد البيانات]
- **الحالة:** [تطوير نشط / صيانة / أرشيف]

## 4. أوامر التشغيل الأساسية (Core Commands)

```bash
# تثبيت الاعتماديات
[install command]

# تشغيل الاختبارات
[test command]

# تشغيل بيئة التطوير
[dev command]

# فحص الأنماط (Lint)
[lint command]
```

## 5. بنية المستودع (Repository Structure)

```
[شجرة المجلدات الأساسية مع شرح موجز لكل مجلد]
```

## 6. قواعد خاصة بهذا المستودع

> أضف هنا فقط قواعد **تنفيذية محلية** (أسلوب كود، اتفاقيات تسمية، مسارات بناء).
> لا تُضف هنا أي إعادة تعريف لمفاهيم الحوكمة (Signal/Decision/SDGM/KFSA) — تلك تعيش في الطبقة الأعلى فقط.

## 7. الوكلاء الفرعيون المفعّلون في هذا المستودع (Active Sub-Agents)

> اذكر أي Sub-Agents من `03-sub-agents/` مفعّلة هنا، ومتى تُستدعى (عند PR، عند طلب صريح، دورياً).

## 8. Routines المفعّلة

> اذكر أي Routines من `05-routines/` مجدولة أو مفعّلة لهذا المستودع.

## 9. Active Runtime Routines

> هذا القسم يربط هذا المستودع فعلياً بطبقة التشغيل (Runtime) الموثَّقة في `claude-operating-system/05-routines/`. لا تُعِد تعريف أي حوكمة هنا — فقط أشِر إلى المراجع.

- **PR Pre-Merge Review Routine** — `claude-operating-system/05-routines/pr-pre-merge-review.md`
- **مراجع Skills المطلوبة:**
  - `claude-operating-system/04-skills/claude-code-pr-review-skill.md`
  - `claude-operating-system/04-skills/product-governance-review-skill.md`
  - `claude-operating-system/04-skills/evidence-pack-builder-skill.md` (عند غياب الأدلة)
- **مراجع الوكلاء المطلوبة:**
  - `claude-operating-system/03-sub-agents/crag.md`
  - `claude-operating-system/03-sub-agents/product-governor.md`
  - `claude-operating-system/03-sub-agents/security-rls-auditor.md` (عند تغيّر ملفات قاعدة بيانات/مصادقة/RLS/Supabase)

> **قاعدة:** لا يجوز دمج أي PR يمس الحوكمة، المصادقة، قاعدة البيانات، عزل المستأجرين (Tenant Isolation)، سلسلة قرار الذكاء الاصطناعي (AI Decision Flow)، أو مصطلحات المنتج، دون مخرجات PR Review Runtime.
